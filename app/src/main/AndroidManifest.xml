<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ddwan.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomView">
        <activity
            android:name=".customeditext.EditTextActivity"
            android:exported="true" />
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".demoanimation.DemoAnimationActivity"
            android:exported="true" />
        <activity
            android:name=".lockpattern.view.LockPattern"
            android:exported="true"></activity>
        <activity
            android:name=".clock.MainActivity"
            android:exported="true" />
    </application>

</manifest>